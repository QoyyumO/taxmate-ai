# Platform configuration for deployment
target_platform=linux
target_arch=x64

# Use npm registry
registry=https://registry.npmjs.org/

# Install optional dependencies
optional=true

# Ignore platform-specific packages that might cause issues
engine-strict=false
